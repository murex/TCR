# Angular 21 upgrade requires legacy-peer-deps due to third-party library compatibility
# This setting ensures npm install/update commands work without manually adding the flag
legacy-peer-deps=true

# Optional: Suppress the npm update notifier
update-notifier=false

# Optional: Set the registry (default npm registry)
registry=https://registry.npmjs.org/
