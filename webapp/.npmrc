# Required because @angular-eslint@21.x declares a peer dep on @angular/cli < 22,
# but this project uses Angular 22. Remove once @angular-eslint releases a v22.
legacy-peer-deps=true

# Optional: Suppress the npm update notifier
update-notifier=false

# Optional: Set the registry (default npm registry)
registry=https://registry.npmjs.org/
