# Required because ng-terminal@6.6.0 declares a peer dep on @angular/common@^21,
# but this project uses Angular 22. Remove once ng-terminal supports Angular 22.
legacy-peer-deps=true

# Optional: Suppress the npm update notifier
update-notifier=false

# Optional: Set the registry (default npm registry)
registry=https://registry.npmjs.org/
