import { FormatTimerPipe } from './format-timer.pipe';

describe('FormatTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
