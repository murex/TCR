# This is goreleaser configuration for releasing tcr-cli application.
# Make sure to check the documentation at http://goreleaser.com
project_name: tcr
before:
  hooks:
    - make tidy
builds:
  - id: "tcr-cli"
    dir: tcr-cli
    binary: tcr
    env:
      - CGO_ENABLED=0
      - SETTINGS_PKG=github.com/murex/tcr-engine/settings
    ldflags:
      - -s -w
      - -X {{.Env.SETTINGS_PKG}}.BuildVersion={{.Version}}
      - -X {{.Env.SETTINGS_PKG}}.BuildOs={{.Os}}
      - -X {{.Env.SETTINGS_PKG}}.BuildArch={{.Arch}}
      - -X {{.Env.SETTINGS_PKG}}.BuildCommit={{.Commit}}
      - -X {{.Env.SETTINGS_PKG}}.BuildDate={{.Date}}
      - -X {{.Env.SETTINGS_PKG}}.BuildAuthor=goreleaser
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - 386
      - amd64
      - arm64
archives:
  - replacements:
      darwin: Darwin
      linux: Linux
      windows: Windows
      386: i386
      amd64: x86_64
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: "{{.Tag}}-snapshot"
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'